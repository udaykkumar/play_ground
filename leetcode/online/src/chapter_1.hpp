#ifndef __CTCI_E6_CHAPTER_1_HXX__
#define __CTCI_E6_CHAPTER_1_HXX__
#include <iostream>

/* 1.1 */
bool is_unique(std::string&);

/* 1.2 */
bool is_one_permutation_of_other_v1(std::string&, std::string&);
bool is_one_permutation_of_other_v2(std::string&, std::string&);

/* 1.3 */
int urlify(char*, size_t);

/* 1.4 */
bool is_Pallindrom_permutation(std::string&);

/* 1.5 */
bool is_one_edit_away(std::string&, std::string&);

/* 1.6 */
char* compress(char *s);

/* 1.7 */
int rotate(int**, const size_t, const size_t);

/* 1.8 */
int zero_matrix(int**, const size_t, const size_t);

/* 1.9 */
bool is_a_rotation(std::string&, std::string&);

#endif
